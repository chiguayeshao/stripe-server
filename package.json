{
  "name": "stripe-server",
  "version": "1.0.0",
  "description": "GasLockR Stripe Server",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.{js,jsx}\""
  },
  "author": "stripe-samples",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "stripe": "^8.191.0"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
